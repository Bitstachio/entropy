namespace Core.Enums
{
    // TODO: Figure out a schema to separate the events
    // This enum can potentially become too large / unspecific
    public enum GameEventType
    {
        GameOver, ScoreUpdated, RockDestroyed, RockHitObject
    }
}