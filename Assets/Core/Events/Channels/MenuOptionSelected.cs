namespace Core.Events.Channels
{
    public struct MenuOptionSelected
    {
        // If appropriate, consider adding a boolean property to indicate if the selected option triggers a scene load
    }
}